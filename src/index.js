import React from 'react';
import { createRoot } from 'react-dom/client';
import AllPage from './slide/AllPage';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AllPage />
  </React.StrictMode>
);
